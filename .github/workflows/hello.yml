name: My First Action  # Name shown in GitHub UI

on: [push]  # Triggers on any push to any branch

jobs:
  test:
    runs-on: ubuntu-latest  # Free Linux runner (kitchen station)
    steps:
    - name: Checkout code
      uses: actions/checkout@v4  # Grabs your repo files

    - name: 23f1002880@ds.study.iitm.ac.in  # Your required step name!
      run: echo "Hello from Poojita's action!"

    - name: List files
      run: ls -la  # Simple extra step to see it work
